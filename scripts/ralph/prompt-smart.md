# Review

Your job is to review the work being done to port Laravel Reverb (PHP) to revurb-ts (Typescript).

## Instructions

Prep:
- 0a. Familiarize yourself with `./scripts/ralph/current.md` and all specs referenced
- 0b. Familiarize yourself with `AGENTS.md` and `README.md`

Review:
- First, read the last review in `./specs/review.md`
    - Revew the work done since your last review.
    - Use `git add -A` and `git commit -m "..."` to commit your changes, then push before reporting back

## Notes

The Laravel Reverb project is located in `./reverb` (gitignored, for reference only) with the port being built in `./`.
