**Review Notes**
- `Factory` now imports the custom HTTP response and instantiates a concrete `ServerProvider`, plus `convertToResponse` detects those responses so controller status codes and headers survive the translation to Bun’s `Response` without runtime errors when checking subscription mode (`revurb-ts/src/Servers/Reverb/factory.ts:29`, `revurb-ts/src/Servers/Reverb/factory.ts:1090`).
- `ChannelConnection` proxies `id()`, `isStale()`, and `disconnect()`, which lets jobs like `PruneStaleConnections` call those methods directly instead of exploding on missing members (`revurb-ts/src/Protocols/Pusher/Channels/channel-connection.ts:119`).
- `UsersTerminateController` now returns the custom JSON response object rather than the Fetch API version, so terminate endpoints emit the intended payload/status and flow cleanly through the factory mapper (`revurb-ts/src/Protocols/Pusher/Http/Controllers/users-terminate-controller.ts:6`, `revurb-ts/src/Protocols/Pusher/Http/Controllers/users-terminate-controller.ts:72`).

**Files Updated**
- `specs/review.md:1` rewritten with `last commit: 840357b92accc9cfaa879191a73d8ba48240b638` and `status: ok`.

Next step: 1) Commit the refreshed review record when ready.